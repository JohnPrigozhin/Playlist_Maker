<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Заголовок экрана -->
    <style name="PlaylistMakerScreenTitle">
        <item name="android:textSize">@dimen/title_text_size</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/ys_display_medium</item>
    </style>

    <!-- Текст пунктов настроек -->
    <style name="PlaylistMakerSettingsItem">
        <item name="android:textSize">@dimen/string_text_size</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/ys_display_regular</item>
    </style>

    <!-- Обложки -->
    <style name="TrackArtwork">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <!-- Кнопки главного экрана -->
    <style name="PlaylistMakerMainButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:backgroundTint">@color/button_bg</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:fontFamily">@font/ys_display_medium</item>
        <item name="android:textSize">@dimen/title_text_size</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:elevation">0dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

</resources>